<%
    ' id of server [set in connectinfo.b]
    ' \v sub-delimeter
    ' # of bars full, 12 total, 0 being empty, 12 being full
    ' \v sub-delimeter
    ' not sure yet, need to check more
    ' \b delimeter
    response.write( "0" & chr("11") & "4" & chr("11") & "344" & chr("8") )
%>